# Simple Jekyll configuration for GitHub Pages
title: "Your Site Title"
description: "Your site description"

# Permalink structure (removes .html extensions)
permalink: pretty

# Plugins (GitHub Pages compatible)
plugins:
  - jekyll-sitemap
